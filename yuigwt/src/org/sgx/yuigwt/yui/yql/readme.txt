in this package are mantained two things, first the YQL, and flia classes for representing the YUI YQl module classes. 

The other thing mantained are the things in the api package, this is: 
java simple overlay types for yql json returned types, for example http://query.yahooapis.com/v1/public/yql?format=json

now just starting with select * from yui.gallery.all, @see api.yui.gallery.all